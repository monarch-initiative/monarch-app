# Data Sources

This section contains documentation for all datasets and ontologies
ingested to create the Monarch Knowledge Graph.

Source documentation is automatically fetched from each ingest repository's README.
To update these pages, run `make fetch-docs`.

Click on a source name in the navigation to learn more about that dataset.
